<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BRiiZE</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.min.css" />

</head>
<body>
    <div class="navbar">
        <div class="container">
            <a href="/" class="name">BRiiZE</a>
            <a href="/new">List your Special Scent</a>
            <a href="/wishList">Wish List</a>
            <a href="/account">Account</a>
            <a href="#">Log Out</a>
        </div>
    </div>
</body>
</html>

<style>
    body { 
        background-color: #f0f0f0; 
        padding-top: 60px;
    }
    .navbar {
        display: flex;
        justify-content: space-between; /* Distributes space between and around items */
        background-color: #333; /* Dark background for the navbar */
        padding: 10px 20px; /* Adds some padding inside the navbar */
        position: fixed; /* Fixes navbar to the top */
        width: 100%; /* Ensures navbar extends full width */
        top: 0; /* Positions navbar at the top of the viewport */
        z-index: 1000; /* Ensures navbar is above other content */
    }

    .container {
        width: 90%;
        margin: auto;
        display: flex;
        justify-content: space-between; /* Evenly spaces links */
    }

    .navbar a {
        color: #fff; /* Light text for contrast */
        text-decoration: none; /* Removes underline from links */
        font-weight: 500;
    }

    .navbar a:hover {
        color: #007bff; /* Highlight color on hover */
        text-decoration: underline; /* Underlines links on hover for emphasis */
    }
    .name{
        font-size: 22px;
    }
</style>