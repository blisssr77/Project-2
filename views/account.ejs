<!DOCTYPE html>
<html lang="en">
<head>
    <%- include ('./partials/head.ejs') %>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account Detail Page</title>
    <body>
        <%- include ('./partials/header.ejs') %>
    
        <div class="container">
            <!--I learned how to make the columns through Skeleton website-->
            <div class="eight columns">
                <div class="card" style="padding: 20px; background: #fff; box-shadow: 0 4px 8px rgba(0,0,0,0.1);">
                    <h4>Account Details</h4>
                    <p><strong>Username:</strong> <%= currentUser.username %></p>
                    <p><strong>Email:</strong> <%= currentUser.email %></p>
                    
                    <form action="/logout" method="POST" style="margin-top: 10px;">
                        <button type="submit" class="button button-primary">Log Out</button>
                    </form>
                </div>
            </div>
        </div>
    </body>
</html>
<style>
    body{
        margin-top: 5%;
    }
</style>
